# coding: utf-8

"""
    FWallet API

    # FWallet API  Multi-tenant financial wallet infrastructure for emerging markets.  **Base URL:** `https://api.fwallet.co.ug`  ## Quick Start for Developers  1. **Create an organization** — `POST /v1/tenants` with a name, slug, and currency config 2. **Get an API key** — `POST /v1/tenants/{id}/api-keys` (save the key — it's shown only once) 3. **Set the header** — Add `X-API-Key: {your-key}` to all subsequent requests 4. **Create wallets** — `POST /v1/wallets` for each user/branch 5. **Fund wallets** — `POST /v1/deposits/momo-webhook` to simulate a MoMo deposit 6. **Transfer money** — `POST /v1/transfers` with an `Idempotency-Key` header 7. **Request payouts** — `POST /v1/payouts` (requires a different person to approve)  ## Key Concepts  - **Multi-tenancy**: Every API call is scoped to a tenant (organization) via the API key. Tenants are fully isolated. - **Double-entry ledger**: Every money movement is recorded as a journal entry with balanced debit/credit lines. Entries are immutable — corrections are posted as reversing entries. - **Money as bigint**: All amounts are stored in the smallest currency unit (e.g. 1 UGX = 1, 1 USD = 100 cents). API responses return these as strings. - **Idempotency**: All POST endpoints that create financial records require an `Idempotency-Key` header. Replaying the same key returns the original result. - **Maker-checker**: Payouts require approval from a person different from the requestor.  ## Error Codes  | Code | Status | Description | |------|--------|-------------| | `INSUFFICIENT_BALANCE` | 422 | Wallet doesn't have enough funds (amount + fee) | | `CURRENCY_MISMATCH` | 422 | Transfer between wallets of different currencies | | `WALLET_NOT_FOUND` | 404 | Wallet ID doesn't exist or belongs to another tenant | | `WALLET_FROZEN` | 422 | Wallet is frozen — can receive but not send | | `INVALID_API_KEY` | 401 | Missing, invalid, or expired API key | | `RISK_CHECK_FAILED` | 422 | Transaction exceeds per-transaction or daily limit | | `MAKER_CHECKER_VIOLATION` | 422 | Cannot approve your own payout request | | `UNBALANCED_ENTRY` | 422 | Journal entry debits don't equal credits (internal error) | | `IDEMPOTENCY_CONFLICT` | 409 | Same idempotency key with different request body |  ## Authentication  Tenant-scoped endpoints accept either bearer API keys via `X-API-Key` or HMAC signed requests via `X-FWallet-Key-Id`, `X-FWallet-Timestamp`, `X-FWallet-Nonce`, `X-FWallet-Content-SHA256`, and `X-FWallet-Signature`. System-level endpoints (`/v1/system/*`) require the `X-System-Key` header. 

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from uuid import UUID
from fwallet.models.api_key_restrictions import ApiKeyRestrictions
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class DeveloperPostDeveloperApiKeys201Response(BaseModel):
    """
    DeveloperPostDeveloperApiKeys201Response
    """ # noqa: E501
    id: UUID
    application_id: Optional[UUID] = Field(alias="applicationId")
    application_name: Optional[StrictStr] = Field(alias="applicationName")
    key_prefix: StrictStr = Field(alias="keyPrefix")
    name: StrictStr
    scopes: List[StrictStr]
    environment: StrictStr
    status: StrictStr
    auth_mode: StrictStr = Field(alias="authMode")
    restrictions: Optional[ApiKeyRestrictions]
    expires_at: Optional[StrictStr] = Field(alias="expiresAt")
    revoked_at: Optional[StrictStr] = Field(alias="revokedAt")
    last_used_at: Optional[StrictStr] = Field(alias="lastUsedAt")
    last_used_ip: Optional[StrictStr] = Field(alias="lastUsedIp")
    last_used_origin: Optional[StrictStr] = Field(alias="lastUsedOrigin")
    created_at: StrictStr = Field(alias="createdAt")
    updated_at: StrictStr = Field(alias="updatedAt")
    key: Optional[StrictStr] = Field(description="The full bearer API key. Only returned once for secret auth mode.")
    signing_secret: Optional[StrictStr] = Field(description="The HMAC signing secret. Only returned once for hmac auth mode.", alias="signingSecret")
    __properties: ClassVar[List[str]] = ["id", "applicationId", "applicationName", "keyPrefix", "name", "scopes", "environment", "status", "authMode", "restrictions", "expiresAt", "revokedAt", "lastUsedAt", "lastUsedIp", "lastUsedOrigin", "createdAt", "updatedAt", "key", "signingSecret"]

    @field_validator('environment')
    def environment_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['live', 'test']):
            raise ValueError("must be one of enum values ('live', 'test')")
        return value

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['active', 'revoked']):
            raise ValueError("must be one of enum values ('active', 'revoked')")
        return value

    @field_validator('auth_mode')
    def auth_mode_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['secret', 'hmac']):
            raise ValueError("must be one of enum values ('secret', 'hmac')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of DeveloperPostDeveloperApiKeys201Response from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of restrictions
        if self.restrictions:
            _dict['restrictions'] = self.restrictions.to_dict()
        # set to None if application_id (nullable) is None
        # and model_fields_set contains the field
        if self.application_id is None and "application_id" in self.model_fields_set:
            _dict['applicationId'] = None

        # set to None if application_name (nullable) is None
        # and model_fields_set contains the field
        if self.application_name is None and "application_name" in self.model_fields_set:
            _dict['applicationName'] = None

        # set to None if restrictions (nullable) is None
        # and model_fields_set contains the field
        if self.restrictions is None and "restrictions" in self.model_fields_set:
            _dict['restrictions'] = None

        # set to None if expires_at (nullable) is None
        # and model_fields_set contains the field
        if self.expires_at is None and "expires_at" in self.model_fields_set:
            _dict['expiresAt'] = None

        # set to None if revoked_at (nullable) is None
        # and model_fields_set contains the field
        if self.revoked_at is None and "revoked_at" in self.model_fields_set:
            _dict['revokedAt'] = None

        # set to None if last_used_at (nullable) is None
        # and model_fields_set contains the field
        if self.last_used_at is None and "last_used_at" in self.model_fields_set:
            _dict['lastUsedAt'] = None

        # set to None if last_used_ip (nullable) is None
        # and model_fields_set contains the field
        if self.last_used_ip is None and "last_used_ip" in self.model_fields_set:
            _dict['lastUsedIp'] = None

        # set to None if last_used_origin (nullable) is None
        # and model_fields_set contains the field
        if self.last_used_origin is None and "last_used_origin" in self.model_fields_set:
            _dict['lastUsedOrigin'] = None

        # set to None if key (nullable) is None
        # and model_fields_set contains the field
        if self.key is None and "key" in self.model_fields_set:
            _dict['key'] = None

        # set to None if signing_secret (nullable) is None
        # and model_fields_set contains the field
        if self.signing_secret is None and "signing_secret" in self.model_fields_set:
            _dict['signingSecret'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of DeveloperPostDeveloperApiKeys201Response from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "applicationId": obj.get("applicationId"),
            "applicationName": obj.get("applicationName"),
            "keyPrefix": obj.get("keyPrefix"),
            "name": obj.get("name"),
            "scopes": obj.get("scopes"),
            "environment": obj.get("environment"),
            "status": obj.get("status"),
            "authMode": obj.get("authMode"),
            "restrictions": ApiKeyRestrictions.from_dict(obj["restrictions"]) if obj.get("restrictions") is not None else None,
            "expiresAt": obj.get("expiresAt"),
            "revokedAt": obj.get("revokedAt"),
            "lastUsedAt": obj.get("lastUsedAt"),
            "lastUsedIp": obj.get("lastUsedIp"),
            "lastUsedOrigin": obj.get("lastUsedOrigin"),
            "createdAt": obj.get("createdAt"),
            "updatedAt": obj.get("updatedAt"),
            "key": obj.get("key"),
            "signingSecret": obj.get("signingSecret")
        })
        return _obj


